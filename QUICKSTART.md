# Быстрый старт деплоя Poiskkino Bot

## Шаг 1: Подготовка секретов

Создайте файл `.env` с вашими настройками (или используйте существующий `example.env` как шаблон).

## Шаг 2: Создание namespace и секрета в Kubernetes

```bash
# Создать namespace
kubectl apply -f infra/namespace.yaml

# Создать секрет из .env файла
kubectl create secret generic poiskkino-bot \
  --from-env-file=.env \
  --namespace=poiskkino
```

## Шаг 3: Деплой

```bash
# Запустить автоматический деплой
./deploy.sh
```

Скрипт выполнит:
1. Сборку Docker образа `mdwit/poiskkino-bot:latest`
2. Push образа в registry
3. Применение всех Kubernetes манифестов

## Шаг 4: Проверка

```bash
# Проверить статус
kubectl get pods -n poiskkino

# Посмотреть логи
kubectl logs -f deployment/poiskkino-bot -n poiskkino
```

## Готово! 🎉

Ваш бот `poiskkinodev_bot` теперь запущен в Kubernetes кластере.

---

Для более детальной информации см. [DEPLOY.md](./DEPLOY.md)
